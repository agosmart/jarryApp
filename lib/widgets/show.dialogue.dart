import 'package:flutter/material.dart';

class CustomDialigue extends StatelessWidget {
  const CustomDialigue({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(),
    );
  }
}
